# 🎯 CNN Hyperparameter Tuning Guide
# =====================================
# استراتژی مرحله‌ای برای رسیدن به AUC ≥ 0.85
# =====================================

## 📊 مراحل تنظیم

### مرحله 1: ایجاد سیگنال قابل تشخیص (COVERT_AMP)
**هدف**: CNN باید حداقل یک pattern واضح ببینه

| COVERT_AMP | انتظار Power Diff | انتظار AUC | توصیه |
|------------|-------------------|------------|-------|
| 0.8        | ~0.5%             | 0.50-0.55  | ❌ خیلی ضعیف - افزایش بده |
| 1.0        | ~1.0%             | 0.60-0.70  | ⚠️ هنوز ضعیف |
| **1.2**    | **~3%**           | **0.75-0.85** | ✅ **شروع خوب** |
| 1.4        | ~5%               | 0.85-0.92  | ✅ قوی - برای debugging |
| 1.6        | ~8%               | 0.90-0.95  | 🎯 خیلی قوی - برای validate |

**توصیه فعلی**: شروع با `COVERT_AMP = 1.2`

---

### مرحله 2: Randomization (ضروری!)
```python
RANDOMIZE_SUBCARRIERS = True  # ✅ حتماً روشن باشه
RANDOMIZE_SYMBOLS = True      # ✅ حتماً روشن باشه
```
**چرا؟** بدون این دو، CNN فقط یک pattern ثابت می‌بینه و overfitting می‌کنه.

---

### مرحله 3: افزودن نویز (بعد از AUC > 0.80)
```python
ADD_NOISE = True
NOISE_STD = 0.01  # شروع با مقدار کم
```

| NOISE_STD | تأثیر | کاربرد |
|-----------|-------|--------|
| 0.0       | بدون نویز | مرحله اولیه debugging |
| 0.01      | نویز خفیف | مرحله اول generalization |
| 0.02      | نویز متوسط | تست واقعی‌تر |
| 0.05      | نویز قوی | سناریوی بدترین حالت |

---

## 🎯 Target Metrics (هدف نهایی)

```
✅ Power Difference: 3-5%
✅ AUC: ≥ 0.85
✅ Precision: ≥ 0.80
✅ Recall: ≥ 0.75
✅ F1 Score: ≥ 0.77
```

---

## 🚀 Workflow پیشنهادی

### ✅ تست سریع (5-10 دقیقه):
```bash
bash quick_test_cnn.sh
```
- 500 samples/class
- 20 epochs
- نتیجه فوری برای تصمیم‌گیری

### 📊 تست کامل (بعد از تنظیم):
```bash
bash run_full_pipeline.sh
```
- 1500 samples/class
- 50 epochs
- نتیجه نهایی

---

## 🔧 Troubleshooting

### اگر AUC < 0.70:
1. ✅ افزایش `COVERT_AMP` به 1.4 یا 1.5
2. ✅ بررسی `RANDOMIZE_*` روشن باشه
3. ✅ بررسی dataset با `quick_stats.py`

### اگر AUC 0.70-0.84:
1. ✅ نگه داشتن `COVERT_AMP = 1.2`
2. ✅ افزایش epochs به 50 یا 70
3. ✅ بررسی overfitting با validation loss

### اگر AUC ≥ 0.85:
1. ✅ کاهش تدریجی `COVERT_AMP` به 1.0
2. ✅ فعال کردن `ADD_NOISE = True`
3. ✅ تست با `NOISE_STD = 0.01`

---

## 📈 مثال جریان کامل

```bash
# Round 1: با COVERT_AMP = 1.2
bash quick_test_cnn.sh
# → بررسی AUC

# اگر AUC > 0.80:
# Round 2: دیتاست کامل
bash run_full_pipeline.sh

# اگر موفق:
# Round 3: اضافه کردن نویز
# در settings.py:
# ADD_NOISE = True
# NOISE_STD = 0.01

bash run_full_pipeline.sh
```

---

## 💡 نکات مهم

1. **Power diff خیلی بالا نره**: اگر > 10% شد، دیگه covert نیست!
2. **Randomization خاموش نکن**: بدون اون CNN یاد نمی‌گیره
3. **نویز رو آخر اضافه کن**: اول بذار مدل بدون نویز یاد بگیره
4. **صبور باش**: ممکنه 2-3 round تنظیم نیاز باشه

---

## ✅ Current Settings (تنظیمات فعلی)

```python
NUM_SAMPLES_PER_CLASS = 1500
COVERT_AMP = 1.2
RANDOMIZE_SUBCARRIERS = True
RANDOMIZE_SYMBOLS = True
ADD_NOISE = False
```

**انتظار**: AUC ~ 0.75-0.85 با 50 epochs
