## 🎯 تغییرات مرحله 3: حل مشکل AUC = 0.5

### ❌ مشکل قبلی:
```
COVERT_AMP = 1.2
Power diff = 0.84%
AUC = 0.50 (تصادفی)
```
**دلیل**: سیگنال خیلی ضعیف + تنوع بی‌رویه

---

### ✅ راه‌حل اعمال شده:

#### 1️⃣ افزایش قدرت سیگنال
```python
COVERT_AMP = 1.4  # هدف: 3-5% power diff
```

#### 2️⃣ تنوع کنترل‌شده (نه کاملاً تصادفی!)
```python
MAX_SUBCARRIERS = 48      # محدود به 48 از 64 subcarrier
NUM_INJECT_SYMBOLS = 7    # همیشه 7 symbol (ولی کدومشون تصادفیه)
```

**منطق**: CNN باید pattern مشابه ببینه ولی با variation کافی

#### 3️⃣ نویز خفیف برای واقع‌گرایی
```python
ADD_NOISE = True
NOISE_STD = 0.01
```

---

### 📊 انتظار نتایج جدید:

| پارامتر | قبل | بعد |
|---------|-----|-----|
| COVERT_AMP | 1.2 | 1.4 |
| Power diff | 0.84% | **~3-5%** |
| AUC | 0.50 | **0.70-0.85** |
| Training time | ~20 min | ~20-25 min |

---

### 🚀 دستورات اجرا:

#### تست سریع (توصیه می‌شه):
```bash
chmod +x quick_test_cnn.sh
./quick_test_cnn.sh
```
- 500 samples (سریع)
- 20 epochs
- نتیجه در 8-10 دقیقه

#### تست کامل:
```bash
chmod +x run_full_pipeline.sh
./run_full_pipeline.sh
```
- 1500 samples (دقیق)
- 50 epochs
- نتیجه در 20-25 دقیقه

---

### 🔍 ابزار جدید: تحلیل قبل از Training

```bash
python3 analyze_power.py
```

این اسکریپت **قبل از training** بهت می‌گه:
- ✅ Power diff چقدره
- ✅ انتظار AUC چقدر باشه
- ✅ آیا ارزش training داره یا نه

---

### 🎯 تفسیر نتایج آینده:

| Power Diff | انتظار AUC | وضعیت | اقدام |
|-----------|------------|--------|-------|
| < 1% | 0.50-0.60 | ❌ خیلی ضعیف | افزایش به 1.6 |
| 1-2% | 0.60-0.70 | ⚠️ ضعیف | افزایش به 1.4 |
| **2-4%** | **0.70-0.85** | ✅ **خوب** | **ادامه** |
| 4-8% | 0.85-0.93 | ✅ عالی | بعداً کاهش بده |
| > 8% | 0.90+ | ⚠️ غیرواقعی | کاهش به 1.2 |

---

### 💡 نکات مهم:

1. **صبر کن تا dataset بسازه**: ~15-20 دقیقه
2. **حتماً تحلیل power رو ببین**: قبل training می‌تونی ببینی آیا موفق میشه یا نه
3. **اگر power diff > 3% شد**: احتمال موفقیت بالاست
4. **اگر AUC > 0.80 شد**: موفق بودیم! می‌تونیم COVERT_AMP رو کم کنیم

---

### ✅ فایل‌های جدید:
- `analyze_power.py` - تحلیل power قبل از training
- `CNN_TUNING_GUIDE.md` - راهنمای کامل تنظیمات
- `quick_test_cnn.sh` - تست سریع
- `run_full_pipeline.sh` - پایپلاین کامل

---

### 🎊 انتظار موفقیت:

با این تنظیمات، باید:
- ✅ Power diff: ~3-5%
- ✅ AUC: 0.75-0.85
- ✅ CNN شروع به یادگیری کنه
- ✅ Precision و Recall بالای 0.7

**وقتی موفق شدیم**, مرحله بعدی:
1. کاهش COVERT_AMP به 1.2
2. افزایش NOISE_STD به 0.02
3. تست در شرایط سخت‌تر
